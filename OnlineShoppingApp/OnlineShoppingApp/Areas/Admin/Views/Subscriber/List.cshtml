@using OnlineShoppingApp.DAL.Entities
@model List<Subscriber>

<div class="col-lg-12">
    <div class="row justify-content-end">
        <div class="">
            <a asp-controller="subscriber" asp-action="create" class="btn btn-secondary">Add new subscriber</a>
        </div>
    </div>
</div>
<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Subscribers</h4>
        </div><!--end card-header-->
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Client Email</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var subscriber in Model)
                        {
                            <tr>
                                <td>@subscriber.Email</td>
                                <td class="text-end">
                                    <a asp-controller="subscriber" asp-action="update" asp-route-id="@subscriber.Id"><i class="las la-pen text-secondary font-16"></i></a>
                                    <form method="POST"asp-controller="subscriber" asp-action="delete" asp-route-id="@subscriber.Id" id="subscriber-form-@subscriber.Id"></form>
                                    <button type="submit" form="subscriber-form-@subscriber.Id">
                                        <i class="las la-trash-alt text-secondary font-16"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div>