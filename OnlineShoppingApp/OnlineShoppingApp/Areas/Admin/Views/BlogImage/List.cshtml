@using OnlineShoppingApp.Areas.Admin.ViewModels.BlogImage
@model ListViewModel

<div class="col-lg-12">
    <div class="row justify-content-end">
        <div class="">
            <a asp-controller="blogimage" asp-action="create" asp-route-blogId="@Model.blogId" class="btn btn-secondary">Add new Blog Image</a>
        </div>
    </div>
</div>
<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Blog Images</h4>
        </div><!--end card-header-->
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Image</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var blogImage in Model.Images)
                        { 
                            <tr>
                                <td><img src="~/assets/img/blog/@blogImage.ImagePath" /></td>
                                <td class="text-end">
                                    <a asp-controller="blogimage" asp-action="update" asp-route-blogId="@Model.blogId" asp-route-Id="@blogImage.Id" ><i class="las la-pen text-secondary font-16"></i></a>
                                    <form method="POST" asp-controller="blogimage" asp-action="delete" asp-route-blogId="@Model.blogId" asp-route-id="@blogImage.Id" id="blog-image-form-@blogImage.Id" ></form>
                                    <button type="submit" form="blog-image-form-@blogImage.Id">
                                        <i class="las la-trash-alt text-secondary font-16"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div> 