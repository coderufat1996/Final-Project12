@using OnlineShoppingApp.DAL.Entities
@model List<Message>

<div class="col-lg-12">
    <div class="row justify-content-end">
        <div class="">
            <a asp-controller="message" asp-action="create" class="btn btn-secondary">Add new message</a>
        </div>
    </div>
</div>
<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">messages</h4>
        </div><!--end card-header-->
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Client Name</th>
                            <th>Client Email</th>
                            <th>Message Subject</th>
                            <th>Message Content</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var message in Model)
                        {
                            <tr>
                                <td>@message.Name</td>
                                <td>@message.Email</td>
                                <td>@message.Subject</td>
                                <td>@message.Content</td>
                                <td class="text-end">
                                    <a asp-controller="message" asp-action="update" asp-route-id="@message.Id"><i class="las la-pen text-secondary font-16"></i></a>
                                    <form method="POST"asp-controller="message" asp-action="delete" asp-route-id="@message.Id" id="message-form-@message.Id"></form>
                                    <button type="submit" form="message-form-@message.Id">
                                        <i class="las la-trash-alt text-secondary font-16"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div>